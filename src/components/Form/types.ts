export type SelectProps = {
	example: string;
};

export type Pokemon = {
	name: string;
	sprite: string;
};

export type FormData = {
	name: string;
	lastName: string;
	pokemon: string;
	pokemons: object[];
};
